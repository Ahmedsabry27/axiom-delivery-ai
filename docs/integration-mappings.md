# Integration mappings

Mappings must be tenant-scoped, versioned, explicitly activated, and limited to safe declarative transformations. Arbitrary scripts are prohibited. Required-field coverage, type compatibility, enum values, identity/entity lookups, duplicate targets, and schema drift must be validated before activation.

Durable entity/field mapping persistence is not implemented in the current increment.
