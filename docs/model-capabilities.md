# Model capabilities

Capabilities shown in the registry are provider-declared metadata. They are distinct from internally evaluated evidence. The capabilities endpoint reports both the declarations and whether evaluation evidence exists, so an unevaluated declaration is not presented as a verified platform capability.
