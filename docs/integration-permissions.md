# Integration permissions

Integration APIs derive tenant and actor context from authenticated claims. Existing permissions separate read, management, connection testing, capability management, agent assignment, and execution. Credentials are resolved through the secret provider; serialized connections expose only whether a reference exists.

Credential-management and expanded EP12 mapping, synchronization, quality, access, and audit permissions require catalogue review before implementation.
