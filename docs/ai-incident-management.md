# AI incident management

AI incidents persist tenant, incident type, severity, status, impacted services/tenant references, trace IDs, owner, impact summary, mitigation, root cause, corrective actions, and timestamps. The operations overview counts unresolved incidents from persisted rows.

Create and update operations are permission checked and tenant scoped. Incident records do not trigger autonomous external remediation; operators retain control of mitigation and closure.
