# Settings permissions

Authenticated users manage their own personal preferences. Workspace, delivery, reporting, retention preview, features, and workspace-wide activity require an existing platform, workspace, or governance administrator group. Tenant identity, roles, groups, secrets, authentication mode, audit, tenant isolation, budget enforcement, and model allowlisting cannot be changed here.
