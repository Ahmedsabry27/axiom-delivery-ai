# Approval delegation

Existing per-approval delegation remains enforced by centralized authorization. A delegate cannot be the requester, cannot decide outside the assigned approval, and still requires approval permissions and a human identity.

Reusable delegation policies with typed/entity/risk scope, start/end dates, expiry, revocation, and immutable use evidence require additional durable persistence and are not complete in this increment.
