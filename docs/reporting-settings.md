# Reporting settings

Reporting settings include fiscal-year start, cadence, trend period, and percentage precision. Calendar changes are versioned and can carry an effective date; prior versions remain in setting history. Display currency does not rewrite stored financial values, and mixed currencies remain separate.
