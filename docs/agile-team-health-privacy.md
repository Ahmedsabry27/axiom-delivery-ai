# Agile team-health privacy

Team health is an aggregated team signal, never an individual productivity score. The backend suppresses the value until at least five anonymous responses exist. Responses below that threshold expose only `INSUFFICIENT_DATA`, response count and the threshold. Raw respondent identity must not be exposed through Agile Performance APIs, logs or exports.
