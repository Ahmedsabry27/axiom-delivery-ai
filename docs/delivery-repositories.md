# Delivery repositories

Repositories require a tenant identifier and apply tenant predicates to list and direct-ID reads. Dependency creation validates distinct endpoints, supported types, existence and tenant ownership. Query helpers cover upstream/downstream relationships and critical-path dependencies. Milestones support tenant-scoped lists and overdue queries.
