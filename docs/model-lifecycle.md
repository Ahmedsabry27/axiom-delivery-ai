# Model lifecycle

Models move through the existing governed lifecycle: `DRAFT`, review/approval states, `ACTIVE`, `DEPRECATED`, `BLOCKED`, and `RETIRED`. Only `ACTIVE` models are eligible for routing. Configuration edits create a new draft configuration version, and a requester cannot approve their own change. Registration and editing never activate a model automatically.
