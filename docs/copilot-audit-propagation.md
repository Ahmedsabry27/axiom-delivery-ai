# Copilot audit propagation

Runtime, budget, action and approval services emit privacy-safe append-only audit events. Saved-insight and prompt lifecycle mutations add identifiers, status, version and evidence counts without message bodies, credentials or excerpts. Correlation and execution identifiers remain available where supplied by canonical runtime flows.
