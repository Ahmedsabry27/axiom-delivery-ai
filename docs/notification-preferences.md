# Notification preferences

Preferences cover assigned actions, approval requests, model incidents, executive briefings, and quiet-hours timezone. Approval and incident notifications are policy locked. The test action is in-app only and does not use or mutate integration credentials. Channel discovery and the complete notification-category matrix remain future work.
