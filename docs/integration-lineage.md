# Integration lineage

Source lineage requires durable tenant/integration/external-ID mappings, canonical entity references, content fingerprints, mapping versions, synchronization timestamps, and conflict policy. Sensitive source payloads should not be stored or exposed by default.

Existing capability usage records do not constitute source-record lineage.
