import { useState } from "react";
import { X } from "lucide-react";
export default function ProposedActionDrawer({proposal,onClose}){const [status,setStatus]=useState("editing");const [content,setContent]=useState(()=>proposal?`Please review and resolve: ${proposal.action}`:"");if(!proposal)return null;return <div className="fixed inset-0 z-[70] flex justify-end bg-stone-950/40" onMouseDown={e=>e.target===e.currentTarget&&onClose()}><section role="dialog" aria-modal="true" aria-labelledby="proposal-title" className="h-full w-full max-w-lg overflow-y-auto bg-white p-6 text-stone-900"><div className="flex justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-[.15em] text-[#a00028]">Human review required</p><h2 id="proposal-title" className="font-display mt-1 text-2xl font-bold">Proposed action</h2></div><button autoFocus onClick={onClose} aria-label="Close proposed action"><X/></button></div><dl className="mt-6 grid grid-cols-2 gap-4 bg-[#f4f1ed] p-4 text-sm"><div><dt className="text-stone-500">Exact action</dt><dd className="mt-1 font-semibold">{proposal.action}</dd></div><div><dt className="text-stone-500">Risk</dt><dd className="mt-1 font-semibold">Controlled draft</dd></div><div><dt className="text-stone-500">Target</dt><dd className="mt-1 font-semibold">Internal review queue</dd></div><div><dt className="text-stone-500">Approval</dt><dd className="mt-1 font-semibold">Required before execution</dd></div></dl><label className="mt-6 block text-sm font-semibold">Draft content<textarea value={content} onChange={e=>setContent(e.target.value)} rows={8} className="mt-2 w-full border border-stone-300 p-3 font-normal"/></label><p className="mt-3 text-sm text-stone-600">No message, calendar event, delivery record, or external system will be changed.</p>{status==="saved"?<p role="status" className="mt-5 bg-emerald-50 p-4 text-sm font-semibold text-emerald-800">Draft saved for review. No external action was executed.</p>:<div className="mt-6 flex flex-wrap gap-3"><button onClick={()=>setStatus("saved")} disabled={!content.trim()} className="bg-[#a00028] px-4 py-2.5 text-sm font-semibold text-white disabled:opacity-50">Save as draft</button><button onClick={()=>setStatus("saved")} disabled={!content.trim()} className="border border-[#a00028] px-4 py-2.5 text-sm font-semibold text-[#a00028] disabled:opacity-50">Submit for future approval</button><button onClick={onClose} className="px-4 py-2.5 text-sm font-semibold text-stone-600">Cancel</button></div>}</section></div>}
