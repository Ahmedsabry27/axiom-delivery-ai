import { useState } from "react";
export default function MessageFeedback({messageId}){const [sent,setSent]=useState(false);if(sent)return <p role="status" className="mt-3 text-xs text-emerald-700">Feedback recorded. Thank you.</p>;return <div className="mt-3 flex flex-wrap gap-2 text-xs text-stone-500"><span>Was this evidence useful?</span>{["Helpful","Not helpful","Missing evidence","Unsafe recommendation"].map(type=><button key={type} onClick={()=>{globalThis.localStorage?.setItem(`axiom.feedback.${messageId}`,type);setSent(true)}} className="underline decoration-stone-300 underline-offset-4 hover:text-[#a00028]">{type}</button>)}</div>}
