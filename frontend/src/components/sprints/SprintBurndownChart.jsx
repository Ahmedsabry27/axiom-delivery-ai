import {CartesianGrid,ComposedChart,Legend,Line,ReferenceLine,ResponsiveContainer,Tooltip,XAxis,YAxis} from "recharts";
export default function SprintBurndownChart({sprint}){const added=sprint.burndown.reduce((sum,p)=>sum+p.added,0),removed=sprint.burndown.reduce((sum,p)=>sum+p.removed,0);const summary=`The sprint started with ${sprint.committed} points. ${added} points were added and ${removed} removed after sprint start. ${sprint.completed} points are complete. The current forecast is ${sprint.forecast} completed points by sprint end.`;return <section className="border border-stone-300 bg-white p-5" aria-labelledby="burndown-title"><div className="flex flex-wrap items-start justify-between gap-3"><div><h2 id="burndown-title" className="font-display text-xl font-bold">Burndown and scope trend</h2><p className="text-sm text-stone-500">Original commitment, actual remaining work and transparent forecast</p></div><span className="bg-[#f4f1ed] px-3 py-2 text-xs font-semibold">Story points</span></div><div className="mt-5 h-72" role="img" aria-label={summary}><ResponsiveContainer><ComposedChart data={sprint.burndown}><CartesianGrid stroke="#ded9d3" vertical={false}/><XAxis dataKey="day" tickFormatter={v=>`Day ${v}`}/><YAxis/><Tooltip/><Legend/><Line name="Ideal" dataKey="ideal" stroke="#d98b00" strokeDasharray="5 4" dot={false}/><Line name="Actual remaining" dataKey="actual" stroke="#a00028" strokeWidth={3}/><Line name="Forecast remaining" dataKey="forecast" stroke="#e65b19" strokeWidth={2} strokeDasharray="4 3"/><ReferenceLine x={sprint.day} stroke="#202020" label="Today"/></ComposedChart></ResponsiveContainer></div><p className="mt-3 text-xs leading-5 text-stone-600">{summary}</p></section>}
