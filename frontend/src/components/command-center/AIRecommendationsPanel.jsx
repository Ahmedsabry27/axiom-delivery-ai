import { BrainCircuit, FileSearch } from "lucide-react";
import StatusBadge from "./StatusBadge";
export function ConfidenceIndicator({ value }) { return <div className="flex items-center gap-2" aria-label={`${value}% confidence`}><div className="h-1.5 flex-1 overflow-hidden rounded-full bg-slate-200"><div className="h-full rounded-full bg-blue-600" style={{width:`${value}%`}}/></div><span className="text-xs font-semibold text-slate-600">{value}%</span></div>; }
export default function AIRecommendationsPanel({ recommendations, onReview, onTakeAction }) {
  return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm lg:p-6" aria-labelledby="recommendations-title"><div className="flex items-center gap-3"><div className="rounded-xl bg-violet-50 p-2.5 text-violet-700"><BrainCircuit className="h-5 w-5"/></div><div><h2 id="recommendations-title" className="text-lg font-semibold text-slate-950">AI Recommendations</h2><p className="text-sm text-slate-500">Evidence-backed actions for your review</p></div></div>
    {recommendations.length === 0 ? <div className="py-10 text-center text-sm text-slate-500">No recommendations available for this period.</div> : <div className="mt-5 space-y-4">{recommendations.map((rec)=><article key={rec.id} className="rounded-xl border border-slate-200 p-4"><div className="flex items-start justify-between gap-3"><h3 className="font-semibold leading-6 text-slate-900">{rec.title}</h3><StatusBadge>{rec.priority}</StatusBadge></div><p className="mt-2 text-sm leading-6 text-slate-600">{rec.explanation}</p><div className="mt-3 flex flex-wrap gap-x-4 gap-y-2 text-xs text-slate-500"><span>{rec.affectedArea}</span><span className="inline-flex items-center gap-1"><FileSearch className="h-3.5 w-3.5"/>{rec.evidenceCount} evidence points</span></div><div className="mt-3"><ConfidenceIndicator value={rec.confidence}/></div><div className="mt-4 flex gap-2"><button type="button" onClick={()=>onReview(rec)} className="rounded-lg border border-slate-300 px-3 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50 focus:outline-none focus:ring-2 focus:ring-blue-600">Review</button><button type="button" onClick={()=>onTakeAction(rec)} className="rounded-lg bg-blue-700 px-3 py-2 text-sm font-semibold text-white hover:bg-blue-800 focus:outline-none focus:ring-2 focus:ring-blue-600 focus:ring-offset-2">Take Action</button></div></article>)}</div>}
  </section>;
}
